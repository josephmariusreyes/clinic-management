## Todo

- Install the following spartan ui components
    - card
    - input
    - button

- Add code to initialize the following modules
    - doctors module
    - patinets module
    - super admin module 
( also add the code for the corresponding components under container )
( declare these components to the module they belong to )
( No need to add any UI yet just write the name of the component ) 

- src\app\shared\layout\public-layout\public-layout.component.html
( for public-layout component add the wrapper here to display the card in the center of the screen use tailwind css to achieve this )

- src\app\features\users\containers\login\login.component.html
( code the login UI )
( No need to add any functionality just add the UI use the input and button component from spartan ui )

## Completed

- Generated the Spartan UI card, input, and button primitives.
- Initialized doctors, patients, and super-admin modules with routing and container component declarations.
- Added lazy-loaded routes for doctors, patients, and super-admin features.
- Centered the public layout content inside a responsive Spartan card using Tailwind CSS.
- Implemented the login UI with email/password inputs and a sign-in button using Spartan UI.
- Validated the application with `npm run build`.
